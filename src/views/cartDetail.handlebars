<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/style.css">
    <title>Carrito</title>
</head>
<body>
    <h1>Carrito</h1>
    <div class="cart-container">
        {{#if cart.products.length}}
            <ul>
                {{#each cart.products}}
                    <li>
                        Producto: {{this.product.title}} <br>
                        Precio: ${{this.product.price}} <br>
                        Cantidad: {{this.quantity}} <br>
                        <form action="/carts/{{../cart._id}}/products/{{this.product._id}}?_method=PUT" method="POST">
                            <input type="number" name="quantity" value="{{this.quantity}}" min="1" required>
                            <button type="submit">Actualizar cantidad</button>
                        </form>
                        <form action="/carts/{{../cart._id}}/products/{{this.product._id}}?_method=DELETE" method="POST" style="display:inline;">
                            <button type="submit">Eliminar</button>
                        </form>
                    </li>
                {{/each}}
            </ul>
            <form action="/carts/{{cart._id}}?_method=DELETE" method="POST">
                <button type="submit">Eliminar todos los productos del carrito</button>
            </form>
        {{else}}
            <p>El carrito está vacío.</p>
        {{/if}}
    </div>
    <a href="/products">Volver a productos</a>
</body>
</html>
